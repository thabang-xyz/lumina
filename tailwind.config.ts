import type { Config } from 'tailwindcss';

const config: Config = {
  theme: {
    extend: {
      fontFamily: {
        marjorie: ['Marjorie', 'sans-serif'],
      },
    },
  },
  plugins: [],
};

export default config;
